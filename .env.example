# Backend configuration (server-side, used by proxy server and Vercel serverless function)
MODEL_NAME=claude-3-5-haiku-20241022

# Local dev only - not needed for Vercel deployment (uses relative paths)
CORS_ORIGIN=http://localhost:5173

# Frontend configuration (client-side, baked into bundle)
# VITE_API_URL - Only needed for local dev to point at the Express proxy.
# In production (Vercel), leave unset — the app uses relative paths automatically.
VITE_API_URL=http://localhost:3001
