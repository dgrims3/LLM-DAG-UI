# Backend configuration (server-side, for proxy server)
MODEL_NAME=claude-3-5-haiku-20241022
# CORS_ORIGIN - Set to your frontend URL in production (e.g., https://your-app.vercel.app)
CORS_ORIGIN=http://localhost:5173

# Frontend configuration (client-side, baked into bundle)
# VITE_API_URL - Set to your backend URL in production (e.g., https://your-api.railway.app)
VITE_API_URL=http://localhost:3001
